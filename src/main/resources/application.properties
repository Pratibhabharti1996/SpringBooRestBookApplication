# multipart configuration
# enables multipart uploads
spring.servlet.multipart.enabled = true
#max size files
spring.servlet.multipart.max-file-size = 3000MB
#threshold after which  files are written tp disk
spring.servlet.multipart.file-size-threshold = 1KB

# JPA configration to db
spring.datasource.name = springjdbc
spring.datasource.url = jdbc:mysql://localhost:3306/springjdbc
spring.datasource.username = root
spring.datasource.password = pratibha
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto =update

